__pycache__/
*.py[cod]
*.pyo
*.pyd

.git/
.gitignore
.gitattributes

notebook/
*.ipynb

venv/
.env

# Large model binaries are NOT tracked in git (see .gitignore), but we DO want
# them included in the Docker image if present locally under app/models/.
# Make sure to copy your .joblib files into app/models/ on the VM before build.


